export class Plane {
  model: string;
  capacity: number;
  maxDistance: number;
}

export class PlanePointer {
  model: string;
  capacity: number;
  maxDistance: number;
}

export class AddPlaneRequest {
  model: string;
  capacity: number;
  maxDistance: number;
}
